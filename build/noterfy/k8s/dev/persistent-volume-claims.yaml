apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: noterfy-pv-local-claim
  namespace: noterfy-dev
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
