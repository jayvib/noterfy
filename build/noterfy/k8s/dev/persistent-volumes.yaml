apiVersion: v1
kind: PersistentVolume
metadata:
  name: noterfy-pv-local
  namespace: noterfy-dev
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/etc/noterfy"
