apiVersion: v1
kind: Service
metadata:
  name: noterfy-svc
  labels:
    app: noterfy
  namespace: noterfy-dev
spec:
  type: NodePort
  ports:
    - port: 50001
      targetPort: 50001
      nodePort: 30001 # the port that will bind to host
      protocol: TCP
  selector:
    app: noterfy
